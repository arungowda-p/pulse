import { Metadata } from 'next';
import './globals.css';
import { AuthNavbar } from '../components/AuthNavbar';

export const metadata: Metadata = {
  title: 'Pulse — Feature Flags',
  description: 'Toggle features without redeploying.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          rel="preconnect"
          href="https://fonts.gstatic.com"
          crossOrigin="anonymous"
        />
        <link
          href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=Plus+Jakarta+Sans:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>
        <AuthNavbar />
        {children}
      </body>
    </html>
  );
}
